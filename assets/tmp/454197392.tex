\documentclass[12pt]{article}
\usepackage{tikz}
\usepackage{subfig}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[margin=1.0in]{geometry}
\usepackage{fancyhdr}
\usepackage{pgf}
\usepackage{qrcode}

\definecolor{mygray}{rgb}{0.8, 0.8, 0.8}

\setlength{\headheight}{65.90973pt}
\addtolength{\topmargin}{5pt}
\pagestyle{fancy}

% ----------- HLAVIČKA ------------
\fancyhead[L]{
    \raisebox{1ex}{\includegraphics[width=2.1cm]{stu-fei-vector-logo.png}}
}

\fancyhead[R]{
    \raisebox{5ex}{
        \scalebox{0.5}{
            \qrcode{https://fei.stuba.sk} % statický QR
        }
    }
}

\fancyhead[C]{
    \parbox{1\textwidth}{
        \centering
        \textbf{Meno: Libor Poracky} \hspace{1.5em}
        \textbf{ID: 12345} \\[0.5em]
        \textbf{Dátum: 21.10.2025} \hspace{1.5em}
        \textbf{Miestnosť: A115} \hspace{1.5em}
        \textbf{Čas: 09:00}
    }
}

% ----------- POMOCNÉ POČÍTADLÁ ------------
\newcounter{rowCounter}
\setcounter{rowCounter}{1}
\newcounter{rowPosition}
\newcounter{blocksNeeded}
\newcounter{remainingQuestions}

% ----------- FUNKCIE ------------
\newcommand{\ceilDiv}[1]{%
    \pgfmathsetmacro{\tempResult}{int((#1 + 19) / 20)}
    \setcounter{blocksNeeded}{\tempResult}
}

% ----------- TU MENÍŠ POČET ODPOVEDÍ ------------
% uprav \foreach cykly nižšie (napr. {1,2,3,4,5} -> {1,2,3,4})
% a rozmery rectangle (napr. 11.5 -> 9.5)

\newcommand{\generateBlock}[2]{
    \setcounter{rowPosition}{1}

    \begin{tikzpicture}[font=\small]
        \draw[thick, black] (-1, -22) rectangle (11.5, -2); % šírka bloku (tu 11.5 pre 5 odpovedí)

        % HLAVIČKA ODPOVEDÍ (A–E)
        \foreach \position/\letter in {1/A, 2/B, 3/C, 4/D, 5/E} {
            \node at ({\position * 2}, -1.5) {\letter};
        }

        % RIADKY
        \foreach \line in {#1,...,#2} {
            \begin{scope}[yshift=-\the\value{rowPosition}cm - 1.5cm]
                \node at (0, 0) {\normalsize\textbf{\the\value{rowCounter}}};
                \foreach \position in {1,2,3,4,5} {
                    \node[mygray] at ({\position * 2},0) {\scalebox{2}{\textbf{$\times$}}};
                    \node[draw, rectangle, inner sep=5pt] at ({\position * 2}, 0) {};
                }
                \ifnum\value{rowPosition}=5
                    \draw[thin, black] (-1, -\the\value{rowPosition} cm + 4.5cm) -- (11.5, -\the\value{rowPosition} cm + 4.5cm);
                \fi
                \ifnum\value{rowPosition}=10
                    \draw[thin, black] (-1, -\the\value{rowPosition} cm + 9.5cm) -- (11.5, -\the\value{rowPosition} cm + 9.5cm);
                \fi
                \ifnum\value{rowPosition}=15
                    \draw[thin, black] (-1, -\the\value{rowPosition} cm + 14.5cm) -- (11.5, -\the\value{rowPosition} cm + 14.5cm);
                \fi
                \stepcounter{rowPosition}
                \stepcounter{rowCounter}
            \end{scope}
        }
    \end{tikzpicture}
}

\newcommand{\generateMultipleBlocks}[1]{
    \setcounter{remainingQuestions}{#1}
    \ceilDiv{#1}
    \foreach \blockIndex in {1,...,\the\value{blocksNeeded}} {
        \pgfmathsetmacro{\startQuestion}{\the\value{rowCounter}}
        \pgfmathsetmacro{\endQuestion}{min(\the\value{rowCounter} + 19, #1)}
        \generateBlock{\number\startQuestion}{\number\endQuestion}
        \addtocounter{remainingQuestions}{-\the\value{rowPosition}}
    }
}

% ----------- DOKUMENT ------------
\begin{document}
\begin{center}
    % Sem meníš počet otázok (napr. 10 alebo 40)
    \generateMultipleBlocks{40}
\end{center}
\end{document}
